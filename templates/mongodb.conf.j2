# mongod.conf

# for documentation of all options, see:
#   http://docs.mongodb.org/manual/reference/configuration-options/

# Where and how to store data.
storage:
  dbPath: {{ graylog_mongodb_data_path }}
  journal:
    enabled: true
{% if graylog_mongodb_version is version ('4.2', '<') %}
  mmapv1:
    smallFiles: true
{% endif %}
#  engine:
#  wiredTiger:

# where to write logging data.
systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log

# network interfaces
net:
  port: 27017
  bindIp: 127.0.0.1


{% if ansible_distribution != "RedHat" %}
processManagement:
  fork: false
{% endif %}

#security:

#operationProfiling:

#replication:

#sharding:

## Enterprise-Only Options:

#auditLog:

#snmp:
